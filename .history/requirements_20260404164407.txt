# ── Core ML / NLP ─────────────────────────────────────────────────────────────
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.0
accelerate>=0.20.0
tokenizers>=0.15.0
safetensors>=0.4.0

# ── Vector search ──────────────────────────────────────────────────────────────
faiss-cpu>=1.7.4

# ── Graph ─────────────────────────────────────────────────────────────────────
networkx>=3.0

# ── Data / utils ──────────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.65.0
requests>=2.31.0
python-dotenv>=1.0.0

# ── Visualization ──────────────────────────────────────────────────────────────
pyvis>=0.3.0
matplotlib>=3.7.0

# ── HuggingFace hub (download model, dataset) ─────────────────────────────────
huggingface_hub>=0.20.0
datasets>=2.14.0

# ── Vietnamese tokenizer (dùng trong train_ner.py để word tokenize tiếng Việt) ─
# Chỉ cần khi train, không cần khi inference
# vncorenlp>=1.0.3   # optional, cần Java

# ── Scraping (news.py / newsurl.py) ───────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=4.9.0
selectolax>=0.3.0